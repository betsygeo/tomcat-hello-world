apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: default
  labels:
    app.kubernetes.io/name: app
    kubernetes.azure.com/generator: container-copilot
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080